{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/luigisaetta/oci-rag-agent-blueprint/schemas/agent-response.schema.json",
  "title": "AgentResponse",
  "description": "Response payload returned by the RAG agent.",
  "type": "object",
  "additionalProperties": false,
  "required": ["conversation_id", "agent_response", "references", "usage"],
  "properties": {
    "conversation_id": {
      "description": "Active conversation identifier that the client can reuse for later requests.",
      "type": "string"
    },
    "response_id": {
      "description": "Responses API response identifier, when available.",
      "type": ["string", "null"]
    },
    "agent_response": {
      "description": "Text generated by the RAG agent.",
      "type": "string"
    },
    "references": {
      "description": "References used or returned by the retrieval layer.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["file_name"],
        "properties": {
          "file_name": {
            "description": "Name of the source file.",
            "type": "string"
          },
          "page": {
            "description": "Page number in the source file, when available.",
            "type": ["integer", "null"],
            "minimum": 1
          },
          "metadata": {
            "description": "Additional metadata returned by the retrieval layer.",
            "type": "object"
          }
        }
      }
    },
    "usage": {
      "description": "Token usage reported by the Responses API, when available.",
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["input_tokens", "output_tokens", "total_tokens"],
      "properties": {
        "input_tokens": {
          "description": "Number of input tokens consumed by the request.",
          "type": ["integer", "null"],
          "minimum": 0
        },
        "output_tokens": {
          "description": "Number of output tokens generated by the response.",
          "type": ["integer", "null"],
          "minimum": 0
        },
        "total_tokens": {
          "description": "Total number of tokens consumed by the request and response.",
          "type": ["integer", "null"],
          "minimum": 0
        },
        "reasoning_tokens": {
          "description": "Number of reasoning tokens, when reported by the model/provider.",
          "type": ["integer", "null"],
          "minimum": 0
        }
      }
    },
    "error": {
      "description": "Optional error message. Null or absent when the request succeeds.",
      "type": ["string", "null"]
    }
  }
}
